export interface AccountTitle {
  id: number;
  value: string;
}

export interface AccountDetail {
  title: string;
  firstName: string;
  lastName: string;
  dateOfBirth: Date;
  email: string;
  mobile: string;
}
